<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/BucketDataLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="0dp"
    android:layout_marginTop="10dp"
    android:background="@color/colorPrimary"
    android:paddingBottom="2dp">


    <ImageView
        android:id="@+id/content_image_soc"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_margin="5dp"
        android:scaleType="fitXY"
        app:srcCompat="@drawable/student_mode" />


    <TextView
        android:id="@+id/content_name_soc"
        style="@style/sub_headings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="-5dp"
        android:layout_toEndOf="@+id/progressBar_bucketdata_eachitem"
        android:paddingLeft="5dp"
        android:text="name"
        android:textAlignment="textStart"
        android:textColor="@android:color/white"
        android:textSize="12dp" />

    <ProgressBar
        android:id="@+id/progressBar_bucketdata_eachitem"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_margin="5dp"
        android:indeterminateTint="@android:color/white" />
</RelativeLayout>